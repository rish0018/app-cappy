# Playwright E2E credentials -- copy to .env.e2e (gitignored) and fill in
# real values. See test_accounts.md (Claude memory) for the two documented
# accounts; never commit real values here.

CAPPY_USER0_EMAIL=
CAPPY_USER0_PASSWORD=
CAPPY_QA_EMAIL=
CAPPY_QA_PASSWORD=
